import React, { useState } from 'react';
import axios from "axios";

export default function Account(props) {
    
    return (
        <section>
            <h2>Test Account</h2>
        </section>
    );
}